/*-------------------------------------------------------
【程序填空】
---------------------------------------------------------
题目：以下程序的功能是:输出a、b、c三个变量中的最小值。
-------------------------------------------------------*/
#include <stdio.h>

main()
{

    int a, b, c, t1, t2;

    scanf("%d%d%d", &a, &b, &c);//&c == [1]
    
    t1 = a < b ? a : b;
    t2 = c < t1 ? c : t1;//c : t1 == [2]

    printf("%d\n", t2);
}